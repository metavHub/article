# 記事リポジトリ

## 構成
- `ai_sd/` Stable Diffusion
- `ai_general/` AI全般
- `design_general/` デザイン論・トレンド
- `design_ai/` デザイン→実装の自動化
- `example_work/` 業務実例（暫定）
- `notes.md` 全記事共通の作業メモ（貼り付け手順・プラットフォーム差分・詰まりポイント）

各記事は `{YYMMDD}_{slug}/` に入れる。
- `source.md` 日本語原稿（note用）
- `en.md` 英語版（Substack / Medium用）
- `assets/` スクショ等の画像。各プラットフォームへは手動アップロード

## 英語版の方針
- 内容は日本語版と1対1で対応させる。情報の追加・削除はしない
- 表記と言い回しのみ英語圏の技術記事の形に寄せる
  - `●` `・` `＊` `▼` は Markdown の見出し・リストに置き換える
  - 日本語特有の挨拶・口語的見出しは英語圏の慣習に合わせる
- 呼称は "Forge"（"WebForge" は使わない）

### タイトル・サブタイトル
en.md の冒頭に、frontmatter としてタイトルとサブタイトルを記載する。形式は以下の通り（`---` で囲む）。

```
---
title: 記事タイトル
subtitle: サブタイトル
---
```

- title は英語の技術記事の慣習に従い、"How to ..." のような動作から始まる形を優先する。日本語版の【】は英語では使わない
- title にコロンは使わず、ダッシュ（—）で受ける。YAML の構文エラーを避けるため引用符が不要になり、frontmatter の値をそのままコピペできるようにするため
- subtitle は本文冒頭を1文に圧縮したもの。Substack / Medium のサブタイトル欄に貼る用
- 本文へのペースト時は frontmatter を含めずにコピーする

日本語版（source.md）にも同様に frontmatter でタイトルを記載する。ただし note にサブタイトル欄がないため、subtitle は記載しない。

```
---
title: 記事タイトル
---
```

### 記号の役割と日英対応
日本語版・英語版それぞれで、以下の記号を使い分ける。

|  | 日本語版 | 英語版 |
| --- | --- | --- |
| 画像キャプション | `※` | `— ` |
| 本文中の注記 | `＊` | `Note: ` |
| 注記内の項目 | `・` | `- `（Markdown 箇条書き） |

- 日本語版の `※` はキャプション、`＊` は本文注記、`・` は注記内の項目を意味する。英語版に変換する際はこの対応で機械的に置き換える
- 例外：`＊` が見出しとして機能している箇所（「＊python がインストールされているかを調べる」等）は `Note: ` に変換せず、英語版でも見出しとして扱う
- 日本語版は記号の後にスペースを入れない（`※任意の場所に…`）。英語版の `— ` `Note: ` `- ` は後ろに半角スペースを入れる
- キャプション行に `- ` は使わない（Markdown の箇条書き記法と衝突するため）。本文中の実際の箇条書きには `- ` を使って問題ない

### リンクの扱い
note の編集画面からテキストをコピーすると、リンク情報が失われる。そのため原稿側で Markdown のリンク記法として保持する。

- 日本語版で「見出し → リンク行」という構造になっている箇所は、見出し自体にリンクを張る形で記載する
  - 例: `・[Python 3.10.9 ダウンロードページ](URL)`
- note は単独行の URL を OG カードとして自動展開するが、Substack / Medium にはこの機能がなくテキストリンクになる。見出しにリンクを張っておけば、同じ文字列が2行並ぶのを避けられる
- URL に不要なクエリパラメータ（GitHub の `?tab=readme-ov-file` 等）は付けない

## 英語版でよくある修正
- 日本語の「実は」は文脈で訳し分ける
  - 読者の予想を裏切る事実の提示 → actually が使える
  - 語調を強めているだけ → 訳さない（動詞の否定形などで足りる）
  - 仕様・事実の説明 → already / in fact など、驚きを含まない語に
- 見出しは名詞句か完全な疑問文にする（"What is X" のような中途半端な形を避ける）
- 日本語の口語的見出し（「〜してたっけ？」等）は引用符付きで直訳しない
- 主語が日本語で省略されている箇所は、英語では明示する

## 注意
- 原稿の内容を勝手に変更しない。提案は会話で行い、承認を得てから書く