# 記事リポジトリ

## 構成
- `ai_sd/` Stable Diffusion
- `ai_general/` AI全般
- `design_general/` デザイン論・トレンド
- `design_ai/` デザイン→実装の自動化
- `example_work/` 業務実例（暫定）
- `notes.md` 全記事共通の作業メモ（貼り付け手順・プラットフォーム差分・詰まりポイント）
- `_shared/` 記事に属さない共通テキスト。`profile.md`（各プラットフォームの自己紹介欄）、`promo.md`（告知文アーカイブ）、`idea-log.md`（記事ネタの記録）

各記事は `{YYMMDD}_{slug}/` に入れる。
- `source.md` 日本語原稿（note用）
- `en.md` 英語版（Substack / Medium用）
- `assets/` スクショ等の画像。各プラットフォームへは手動アップロード

## 原稿の下準備
note からコピーした source.md には不可視文字が混ざる。en.md を作る前に以下を除去する。文字としての本文内容は変えない。
- ノーブレークスペース（U+00A0）→ 削除
- 行末の全角スペース（U+3000）→ 削除
- 連続する空行 → 1つに圧縮（画像を貼っていた箇所の名残）
- 半角スペースが2つ以上並んでいる箇所 → 1つに

除去後に報告する。判断が必要な変更（語句の修正等）はここに含めず、会話で確認する。

## 英語版の方針
- 内容は日本語版と1対1で対応させる。情報の追加・削除はしない
- 表記と言い回しのみ英語圏の技術記事の形に寄せる
  - `●` `・` `＊` `▼` は Markdown の見出し・リストに置き換える
  - 日本語特有の挨拶・口語的見出しは英語圏の慣習に合わせる
- 呼称は "Forge"（"WebForge" は使わない）

### タイトル・サブタイトル
en.md の冒頭に、frontmatter としてタイトルとサブタイトルを記載する。形式は以下の通り（`---` で囲む）。

```
---
title: 記事タイトル
subtitle: サブタイトル
---
```

- title は英語の技術記事の慣習に従い、"How to ..." のような動作から始まる形を優先する。日本語版の【】は英語では使わない
- title にコロンは使わず、ダッシュ（—）で受ける。YAML の構文エラーを避けるため引用符が不要になり、frontmatter の値をそのままコピペできるようにするため
- subtitle は本文冒頭を1文に圧縮したもの。Substack / Medium のサブタイトル欄に貼る用
- 本文へのペースト時は frontmatter を含めずにコピーする

日本語版（source.md）にも同様に frontmatter でタイトルを記載する。ただし note にサブタイトル欄がないため、subtitle は記載しない。

```
---
title: 記事タイトル
---
```

### 記号の役割と日英対応
日本語版・英語版それぞれで、以下の記号を使い分ける。

|  | 日本語版 | 英語版 |
| --- | --- | --- |
| 画像キャプション | `※` | `— ` |
| 本文中の注記 | `＊` | `Note: ` |
| 注記内の項目 | `・` | `- `（Markdown 箇条書き） |

- 日本語版の `※` はキャプション、`＊` は本文注記、`・` は注記内の項目を意味する。英語版に変換する際はこの対応で機械的に置き換える
- 例外：`＊` が見出しとして機能している箇所（「＊python がインストールされているかを調べる」等）は `Note: ` に変換せず、英語版でも見出しとして扱う
- 日本語版は記号の後にスペースを入れない（`※任意の場所に…`）。英語版の `— ` `Note: ` `- ` は後ろに半角スペースを入れる
- キャプション行に `- ` は使わない（Markdown の箇条書き記法と衝突するため）。本文中の実際の箇条書きには `- ` を使って問題ない

### リンクの扱い
note の編集画面からテキストをコピーすると、リンク情報が失われる。そのため原稿側で Markdown のリンク記法として保持する。

- 日本語版で「見出し → リンク行」という構造になっている箇所は、見出し自体にリンクを張る形で記載する
  - 例: `・[Python 3.10.9 ダウンロードページ](URL)`
- note は単独行の URL を OG カードとして自動展開するが、Substack / Medium にはこの機能がなくテキストリンクになる。見出しにリンクを張っておけば、同じ文字列が2行並ぶのを避けられる
- URL に不要なクエリパラメータ（GitHub の `?tab=readme-ov-file` 等）は付けない

## 英語版でよくある修正
- 日本語の「実は」は文脈で訳し分ける
  - 読者の予想を裏切る事実の提示 → actually が使える
  - 語調を強めているだけ → 訳さない（動詞の否定形などで足りる）
  - 仕様・事実の説明 → already / in fact など、驚きを含まない語に
- 見出しは名詞句か完全な疑問文にする（"What is X" のような中途半端な形を避ける）
- 日本語の口語的見出し（「〜してたっけ？」等）は引用符付きで直訳しない
- 主語が日本語で省略されている箇所は、英語では明示する

## 告知文
告知文は `_shared/promo.md` に集約する。記事ディレクトリには置かない。

- カテゴリごとに `##` で区切る。見出しは「構成」の表記に合わせて `ai_sd：Stable Diffusion` の形にする
- カテゴリ内は日付の新しいものを上に追加する
- 各記事は `###` 見出しに日本語タイトルを記載し、その下に公開日を書く
- 各プラットフォームは `####` 見出しで分ける（LinkedIn / X（日本語）/ X（英語））

### 告知文の方針
告知文の粒度は、記事の想定読者に合わせて決める。一律の正解はない。現時点では以下の3分類を候補として置く。運用しながら調整・追加していく。

- 初心者向け・入門記事 → 技術的詳細は出さず、「何ができるようになるか」を伝える。専門用語を減らし、読み手の状況に寄せる
- 一般向け → 抽象度は高めだが、入門とは異なる。記事の一番の差別化点を1つだけ出す
- 技術寄り・専門記事 → 具体的な技術要素を前に出す。対象読者は用語で記事の内容を判断するため、抽象化すると素通りされる

判断に迷う場合は、「この告知を見た人が記事を開いて、期待と中身が一致するか」で決める。

共通して守ること:
- 記事全体の要約にはしない。読む動機を作るのが役割
- Medium のリンクは貼らない。canonical が Substack を向いているため、外部からの導線は Substack に集約する
- X は日本語140字 / 英語280字制限。URL は11.5字換算
- LinkedIn は3行目以降が折りたたまれるため、冒頭2文で要点を出す

## 作業ログ
作業中に気づいたことを `_shared/idea-log.md` に追記する。記録の時点では構造化せず、平坦に書き足していく。起点・経過・着地の整理は、記事を書く段階でまとめて行う。

### 形式
`- [YYYY-MM-DD] #対象 #性質 内容`

### 性質タグ
- `#起点` — 何をやろうとしたか、なぜそれをやろうと思ったか
- `#詰まり` — 詰まった箇所、エラー、想定と違った挙動。エラーメッセージは本文に書く
- `#発見` — 原因や仕組みが分かったが、まだ解決には至っていない。また、詰まり・解決のどちらにも属さない気づき。ここから別の取り組みが派生する可能性があるため、分類に迷ったものは捨てずにここに入れる
- `#解決` — 最終的にどう解決したか
- `#方針変更` — 当初の目的から方針を変えた場合。その理由も書く

`#発見` と `#解決` の両方に該当する場合は `#解決` を優先する。

### 対象タグ
作業対象を1〜2語で表す。`#figma` `#mcp` `#css` など。
既存のタグがあれば表記を揃える。事前に候補を限定せず、新しい対象が出たら新規に付けてよい。

### 判断基準
「同じことで次も詰まりそうか」「他の人も詰まりそうか」。
記事にできるかどうかは判断しなくてよい。材料として残すことが目的。

特に、当初の目的と最終的な着地がずれた場合は必ず記録する。判断の経緯が含まれるため、記事の材料として価値が高い。

## 注意
- 原稿の内容を勝手に変更しない。提案は会話で行い、承認を得てから書く