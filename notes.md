# 作業メモ

## 貼り付け手順（Substack / Medium 共通）
1. VS Code で en.md を開く
2. Ctrl + Shift + V で Markdown プレビューを表示
3. プレビュー側のペインをクリックしてフォーカスを移す
4. Ctrl + A → Ctrl + C
5. 貼り付け先の本文に通常ペースト（Ctrl + V）

※ Markdown をそのまま貼るとコードブロック化するか、記法が文字として残る
※ Ctrl + Shift + V（書式なし貼り付け）も不可

## プラットフォーム差分

### Substack
- 単独行の URL は OG カードにならない（note と異なる）
  → 見出し自体にリンクを張る形にする
- コードブロック末尾に空行が入る
  → 原因: markdown-it が `<pre><code>...\n</code></pre>` を出力するため
  → 原稿側では制御不可。貼り付け後に手動削除（10箇所程度）
  → 自動化するなら: en.md → en.html を生成するスクリプトで末尾改行を除去

### Medium
- ネストしたリストに非対応
  → 貼り付け時に空の項目が生まれる
  → 対処: 空項目を削除し、Shift + Enter で親項目内の改行にする
- 本文1行目がタイトル、2行目がサブタイトルとして扱われる
- 見出しは2段階（Large / Small）のみ
- WebP を受け付けない（Substack は可）
  → note からダウンロードした画像は WebP になるため PNG 変換が必要
- 画像キャプション欄あり（note と同様、Substack にはなし）
  → en.md は Substack 基準で統一するため、キャプション欄は使わず `— ` 付きの本文行で代替
- インポート機能で Substack 公開記事を取り込める（canonical が自動で Substack を向く）
  → 手動での本文貼り付け・画像アップロードは不要になる見込み
  → 未検証: Substack 上の WebP 画像がインポート時に正しく取り込まれるか
  → 検証は Substack 公開後。手動版の下書きは残しておき、比較して採用を決める

## 運用方針
- 公開順は note → Substack → Medium。Medium のインポートが Substack の公開記事を起点にするため、Substack を先に公開する
- 執筆時はスクショを note の編集画面に直接貼り付ける（思考を止めないため優先）
- 画像の回収・変換は後工程でまとめて処理する
  → 自動化候補: note から DL した WebP を一括で PNG 変換するスクリプト

## その他
- スクショを note に直接貼ると元ファイルが残らない
  → note から DL して assets/ に回収する（WebP なので PNG 変換が必要）
- 画像は Substack / Medium ともに手動アップロードが必要（API なしでは自動化不可）
